# Web builder (packer/web_builder) runtime deps. The map packer itself is the
# native `obc-pack` binary (firmware/obc-pack) — no Python packing deps needed.
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
requests>=2.28.0          # Geofabrik index + PBF downloads
shapely>=2.0.0            # simplify region-picker boundary polygons (geofabrik.py)
