# TS2 docs-only selection probe

This temporary document exercises the pull-request selector and aggregate gate with a change that
requires only the documentation and always-run policy surfaces. The probe pull request is closed
without merging after its GitHub Actions evidence is recorded on TS2.
