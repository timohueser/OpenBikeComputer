# critic
## missed
- Crate obc-sensors (1,141 LOC, 30 inline #[test]: ubx 14, bmp581 6, compass 5, icm20948 5) and its registry suite rust.obc-sensors (testing/suites.toml, level unit, surface device-drivers). No survey inventories it. `firmware/obc-sensors/src/{ubx,bmp581,icm20948,compass}.rs` — It is the only host-testable code for the GPS/baro/IMU drivers that run on the board; any HIL-modular design must know these driver parsers are already host-tested so the hardware slot is only bus timing.
- Crate obc-display and suite rust.obc-display (level contract, cadence_conflict for the #[ignore] timing probe at rowdiff.rs:342). Only the maps survey mentions it in a duplication row (MiniTarget/MiniOverlay); nobody inventories the FAR-12/13 conformance suite that runs the board's FLPR presenter half (SpanPresenter, RowDiff, composite_into_resident) on a host double with only the transport faked. `firmware/obc-display/tests/display_contracts.rs (580 LOC, 12 tests) + 60 inline tests; src/ls021/rowdiff.rs:342` — This is the repo's existing, working example of the 'extraction-first, hardware keeps only the transport' pattern that #1262 mandates for the board; the redesign should copy it for ride.rs rather than invent a new seam.
- 3,056 LOC of on-device harness/bench/repro binaries inside the board crate. flat_store_bench is CI-built as the frame-ceiling ELF (ci.yml:590-600, resource_guard frames --match obc_storage::flat); ble_central_repro's epic #707 CLOSED 2026-07-17 and display_test's #1158 CLOSED 2026-08-27, both with no death trigger applied. README.md:12 refers to `ls021-*` binaries that do not exist (0 files). The board suite's `cargo build --release` compiles all three bins (no [[bin]] required-features). `firmware/obc-fw-nrf54l/src/bin/{ble_central_repro.rs 347, display_test.rs 181, flat_store_bench.rs 2528}` — The device-system survey says the board crate 'has 0 tests'; it actually carries the largest bench/scaffolding block in the repo, one of which is load-bearing for a CI gate. The bench-separation and scaffolding-stripped rules apply here and nobody flagged it.
- Registry suites ci.licenses (extra_triggers Cargo.lock/package-lock/about.toml) and ci.docs (pull_request=always, `--check-links`) plus the cargo-deny job are never surveyed. `tools/licenses/gen-third-party.sh (122 LOC), docs/build_docs.py (789 LOC), ci.yml `deny` job (4 manifests)` — ci.docs is one of the 13 `always` suites; the glance/organization design needs to classify these as guards, and the infra survey's guard consolidation counts 7 scripts when there are at least 9 guard-shaped steps.
- Release-time verification (tag<->crate version guard, 'Refuse a real release that still trusts the committed TEST key', obc-mkimage wrap --sign-seed + inspect gate, manifest generation) and the site deploy that rebuilds trunk demo + wasm bridges + builder outside ci.yml. Not in any survey or in testing/suites.toml. `.github/workflows/release.yml, deploy-site.yml` — 'When tests run' has a release cadence the registry does not model; the plans survey notes 0 `release` scheduled suites but nobody looked at what release.yml already gates.
- Inline tests of the host flat-store adapters (map + route repositories over obc-storage). The device-system survey counts 26 host-core inline tests but never names these; the maps survey only cross-references flat_store.rs as 'a third in-memory card'. `host/obc-host-core/src/flat_map/tests.rs (229 LOC, 5 tests), src/flat_routes/tests.rs (118 LOC, 3 tests), src/flat_store.rs (tempfile card)` — These are the existing host-side tests of the card boundary the maps survey proposes to build a system test across (pack -> flat-store admission -> StoreSource -> App).
- Five of the 22 obc-app integration binaries are absent from the device-system per-file census (it names ~14). ble.rs is the host->app BLE state seam; host_protocol.rs is 'what a host observes of the app's remaining protocol surface'. `firmware/obc-app/tests/{ble.rs 10 tests/233 LOC, host_protocol.rs 4/82, hold_hint.rs 1/76, marker.rs 2/72, rain.rs 5/288}` — The binary-consolidation and 'move pure-function files inline' proposals need the full list; hold_hint/marker are 72-76 LOC render wiring tests that are whole link steps today.
- The infra survey lists the tests of these tools (python.repository-tools, pull_request=always) but not the tools themselves as deletion candidates, although the owner ruled per-merge LOC bookkeeping dead (memory review-round-economics §11). `tools/loc_ledger.py (862), tools/loc_report.py (352), tools/dev_cleanup.py (394), tools/docs_copy.py (173), tools/weather-service.sh (107) + tools/tests/test_loc_ledger.py (363), test_loc_report.py (80)` — ~1,650 LOC of scripts + ~440 LOC of tests run on every PR for a policy the owner has retired.
- Untracked local clutter that a `find -name '*test*'` walk hits: a stale full worktree (git worktree list shows it plus a stale external worktree and OSM-agents/gps1603), an empty copydesk/tests, an Xcode DerivedData tree. `.claude/worktrees/issue-1515-blockers-17d0fb (full checkout at d42e2e3a), copydesk/ (untracked, only __pycache__), companion-ios/DerivedData, companion-ios/EchoHarness` — Any LOC census that does not prune .claude/ double-counts every test file; the 27 GB target/ and 98%-full disk the build-time survey reports are partly this.
## contradictions
- A: infra: replace testing/suites.toml + tools/suite_registry.py + coverage-policy.toml + ci_aggregate.py with a ~12-row job map (loc_delta -3,300), drop the level taxonomy as a CLI dimension, drop coverage scaffold.
  B: plans-decisions: L1 (seven-level taxonomy), L2 (per-suite selection, split never relabel), L3 (single registry of non-derivable facts), L4 (one selection engine, no second selector), L6 (fixed local interface obc test unit|component|contract|fixtures|e2e), L8 (coverage ratchet for the safety core) are LOCKED decisions from #1449/#1450/#1451/#1465 that a redesign 'must respect or explicitly ask the owner to overturn'.
  verdict: Real conflict, unreconciled by either survey. The infra evidence is verified (ci.yml:284 `cargo nextest run --workspace --all-features … -E 'not package(obc-wx-bake)'`; ~770 of 1,654 selector lines exist to prove the regex-parsed workflow agrees with the plan; TEST_POLICY_PATTERNS suite_registry.py:63-78 includes CLAUDE.md/AGENTS.md/justfile). The owner said old conventions may be overthrown, but no survey puts the specific #1449 decisions L1-L6/L8 in front of him as the thing being overthrown. Needs an explicit ruling before any implementation.
- A: device-system: the UI sweep is '233 separate obc-sim process launches (grep -c "$SIM" = 233)'.
  B: maps-host-tools: '~300 headless obc-sim process launches'; build-time: '208 render invocations, 317 manifest lines'.
  verdict: Verified: `grep -c '"$SIM"' firmware/ui-snapshots.sh` = 233 literal invocations; firmware/ui-snapshots.sha256 has 317 rows (some invocations sit in loops). The 208 and ~300 figures are not reproducible; use 233 launches / 317 frames.
- A: ci.yml:234 and :374 (quoted by device-system, contracts, infra): 'all 191 test binaries'.
  B: build-time (measured): `cargo test --workspace --all-features --no-run` produces 159 executables (49 unit harnesses + 110 integration); cargo metadata --no-deps sums to ~112 integration targets across 38 members.
  verdict: 159 is the measured current number; the 191 in ci.yml is a stale comment. Surveys that repeat 191 are quoting the comment, not counting.
- A: Given facts: firmware inline #[cfg(test)] LOC = 43,761.
  B: device-system (brace-matched count): 31,748 for all of firmware/, of which obc-app 21,701 + 3,529 harness.
  verdict: Method difference (the survey says so itself); a ~12k LOC gap in the headline test-LOC number. Any 'reduce testing LOC' target must name one counting convention first. Not re-derived here.
- A: device-system: 'Board crate obc-fw-nrf54l (no tests) … loc 0, test_count 0'; contracts: 'obc-boot … zero tests'.
  B: firmware/obc-fw-nrf54l/src/bin/ holds 3,056 LOC of on-device bench/repro/wiring harness (flat_store_bench 2,528, ble_central_repro 347, display_test 181), and ci.yml:594 builds flat_store_bench as the resource-guard frame-ceiling ELF.
  verdict: Both true for `#[test]` (0), but the 'board crate is a build, not a test' framing misses that the board crate's CI build IS a measurement gate over a bench binary, and that two other bins are dead scaffolding (their issues #707 and #1158 are CLOSED per gh).
- A: weather: nowcast_skill_events.rs has 'ONE assert (line 820)' in one test and 'ZERO asserts' in the other.
  B: grep: two real assert! calls (line 302 in a helper method, line 820) plus one comment (327).
  verdict: Minor; the substance (a 36 s, 875-LOC measurement harness with essentially one product assertion) holds.
- A: ios: '28 Task.sleep sites across 12 files'.
  B: grep -r 'Task.sleep' companion-ios/Packages/OBCKit/Tests | wc -l = 30.
  verdict: Minor count drift (two lines may be comments/duplicates); the point stands.
- A: plans-decisions L2 / CLAUDE.md:50-51 / #1448 §4: 'select whole suites, never individual test functions'.
  B: CONTRIBUTING.md:20 shows `obc test -p obc-app weather_alert`; tools/justfile:611 passes "$@" through to cargo test; docs/testing.md:117 shows a test-name filter.
  verdict: Documented contradiction inside the repo (plans survey flags it). The redesign must state whether the rule is about CI/registry selection only.
- A: device-system inventory row 'In-crate staging harnesses' = 2,821 LOC (screens+upload+quick_drawer+mod).
  B: device-system how_it_works: '3,529 LOC of in-crate harness'.
  verdict: Consistent once support.rs (708) is added, but the two numbers appear side by side without saying so; use 3,529 (includes support.rs) when comparing with tests/ 6,122.
- A: Given facts: 'obc-app … 23 files' under tests/.
  B: device-system / build-time: 22 tests/*.rs binaries.
  verdict: Verified: `ls firmware/obc-app/tests` = 22 .rs files + common/ directory = 23 entries. 22 binaries is the number that matters for link cost.
- A: web-desktop: obc-desktop has 27 tests.
  B: A plain `#[test]` grep gives 25.
  verdict: 27 is right: 25 #[test] + 2 #[tokio::test] (usb/runtime_guard.rs).
- A: ios registry entry ios.xcuitest command uses destination 'iPhone 16 Pro' (suites.toml:653).
  B: companion-ios/scripts/capture-website-screenshots.sh:8 defaults to 'iPhone 17 Pro'.
  verdict: Verified; the registered command is not the one CI runs (script:80 `-only-testing:OBCCompanionUITests/WebsiteScreenshotTests`).
## verified_claims
- contracts: the S0 whole-object transfer protocol (obc_ble Receiver/StreamSender/TransferControl/TransferStatus/ListHeader/RouteListEntry/TripListEntry/HeldMagic) has no production user outside firmware/obc-ble and can be deleted (~-2,100 LOC).
  verdict: CONFIRMED (with a scope caveat)
  evidence: `grep -rlE 'Receiver::new|StreamSender|TransferControl\b|TransferStatus\b|ListHeader|RouteListEntry|TripListEntry|HeldMagic' --include='*.rs' firmware host apps | grep -v firmware/obc-ble/` returns nothing (this includes the standalone board crate under firmware/). The board's obc_ble:: uses are only StatusMessage/SetClock/CommandResult/CommandStatus/WeatherUnchanged, WeatherRefresh/DueScheduler/WeatherRequestContext, classify_advertisement + parse_* sensor decoders, Crc32, install_fw_reply. companion-ios GATT.swift:38 marks transferControl retired. LOC: obc-ble/src transfer.rs 278 + list.rs 360 + descriptor.rs 1,095 (only the ObjectType/Op/Transfer* part is dead) + tests transfer 181 + list 200 + dfu 126 + ~14 tests in tests/vectors.rs + obc-vectors builders lib.rs:425-660. The -2,100 figure is plausible only if descriptor.rs is split, not deleted whole.
- web-desktop / ios: the real Rust engine + simulated card (obc_link::flat::Engine + obc_storage::flat::FlatStore<SparseDisk>) is wasm-buildable today, so one virtual device can replace the TS MockDevice (982 LOC) and the Swift scripted links.
  verdict: CONFIRMED
  evidence: `cargo tree -p obc-storage --features std --target wasm32-unknown-unknown -e normal` resolves to byteorder, embedded-hal, embedded-io, embedded-sdmmc (timohueser fork, no defaults), hash32, heapless, obc-crc, obc-formats, obc-link, obc-ports, stable_deref_trait — pure Rust, no native deps. `cargo tree -p obc-link --features std` adds only sha2/digest/typenum. The producer module is `#[cfg(any(test, feature = "std"))] pub mod vectors;` (firmware/obc-link/src/flat/mod.rs:26-27). Not verified: whether obc-link's Ceilings/Policy/OpenPolicy express every MockDeviceOptions knob (the survey itself lists this as open).
- infra / maps / device-system / build-time: CI selection is job-granular for Rust — any selected Rust suite runs the whole workspace, and the UI sweep + obc-bench release build run unconditionally inside the `test` job regardless of what changed.
  verdict: CONFIRMED
  evidence: .github/workflows/ci.yml:284 `cargo nextest run --workspace --all-features --locked --no-tests fail -E 'not package(obc-wx-bake)'`; :313 `cargo run -p obc-bench --release --locked -- --check host/obc-bench/golden.txt` and :321-325 `cargo build -p obc-sim … firmware/ui-snapshots.sh … ui_snapshot_manifest.py check` are plain steps with no `if:`; :338 `cargo build -p obc-pack --release` + pytest also inside `test`; test-weather legs each pass `--workspace --all-features` (ci.yml:392-397). Locally `obc check test` is serial `cargo test --workspace --all-features` + the bench release run (tools/justfile:694-702), not nextest.
- build-time / maps: integration-binary fan-out is the inner-loop cost driver; consolidating each crate's tests/*.rs into one tests/main.rs cuts obc-app from 23 to 2 link steps (measured bound 2.9 s for --lib --test nav vs 12.7 s today).
  verdict: CONFIRMED structure; time delta is measured-bounded, not measured
  evidence: cargo metadata --no-deps: integration test targets obc-app 22, obc-route 15, obc-render 11, obc-pack 8, obc-ble 6, obc-dfu 6, obc-reader 6, obcm-assemble 5, obc-dem 4 … (~112 total); firmware/obc-app/tests has 22 .rs + common/. The 4-5 s post-consolidation estimate rests on the build-time survey's own timing of `--lib --test nav` (2.88 s) plus a guessed +1-2 s for the merged crate; the ~2 s/binary macOS first-launch cost (44 s/edit) is measured only in that agent's shell and still needs the owner's own terminal to confirm.
- contracts / build-time: focused test builds are inflated by dev-deps — obc-ble pulls 25 crates (2 normal) via obc-vectors; obc-app pulls host/obc-dem with default features for one string constant; obc-route pulls obc-pack (libGEOS) for two test files.
  verdict: CONFIRMED
  evidence: `cargo tree -p obc-ble -e normal,dev` = 25 unique crates vs `-e normal` = 2. firmware/obc-app/Cargo.toml [dev-dependencies] = obcm-testkit + obc-dem (no default-features=false); the only use is firmware/obc-app/src/screen/settings/about.rs:211 `assert_eq!(joined, obc_dem::COPERNICUS_ATTRIBUTION)`. firmware/obc-route/Cargo.toml [dev-dependencies] obc-pack, obcm-testkit, obc-fixtures; `obc_pack` appears only in tests/nav.rs and tests/detour.rs.
- plans-decisions: tools/s6b_board_cutover_soak.py is past its death trigger ('delete this file when #1494 closes').
  verdict: CONFIRMED, and extended
  evidence: gh: #1494 CLOSED 2026-08-26. Also newly found: firmware/obc-fw-nrf54l/src/bin/ble_central_repro.rs (repro for #707, CLOSED 2026-07-17) and display_test.rs (#1158, CLOSED 2026-08-27) have the same status with no trigger recorded; #91 (HIL testrack) is CLOSED-completed with none of its deliverables present (no headless/--script in host/obc-usb-host).
- maps / weather / ios: the registry `fixtures` field is free text not validated against fixtures/catalog.toml; profile `test` downloads sim-peak-view (103 MB) although no test reads it.
  verdict: CONFIRMED
  evidence: suites.toml fixture values: test, sim, checked-in-assembly, checked-in-web-fixtures, captured-opera, weather-event-packs, captured-met, legacy-library, captured-dem, live-copernicus-manual, builder-corpus; catalog.toml package ids: sim-grimsel, sim-peak-view, sim-monaco, weather-dwd-icon, weather-noaa, weather-event-derecho, weather-event-airmass; profiles sim/weather/test. fixtures/catalog.toml:89-92 `[profiles.test] packages = ["sim-peak-view", weather-*]`.
- contracts: specs/vectors/manifest.json (1,632 lines) is hand-maintained; no Rust code writes or verifies it.
  verdict: CONFIRMED
  evidence: `grep -rn manifest.json host/obc-vectors/src host/obc-vectors/tests` hits only two comments (lib.rs:715, tests/vectors.rs:560); specs/vectors/README.md:203-207 'Regenerating … after a deliberate spec change' instructs a manual update.
- ios: 75 of 77 XCUITest methods have no CI route; only WebsiteScreenshotTests runs.
  verdict: CONFIRMED
  evidence: `grep -rc 'func test' companion-ios/OBCCompanionUITests/*.swift` sums to 77; companion-ios/scripts/capture-website-screenshots.sh:80 `-only-testing:OBCCompanionUITests/WebsiteScreenshotTests`; ci.yml:912 is the only xcodebuild test entry; .github/workflows contains bake, ci, deploy-site, release, test-exception-health, wx-freshness — none schedules xcodebuild test.
- web-desktop: python.builder can pass vacuously (skipif when obc-pack binary or corpus is missing); sha256.test.ts hashes 600 MB.
  verdict: CONFIRMED
  evidence: builder/tests/test_routing_profiles.py:52-55 `requires_pack = pytest.mark.skipif(_pack_bin() is None or not os.path.exists(TINY_PBF), …)`; builder/app/src/lib/device/sha256.test.ts lines ~66-73 `const megabytes = 600` with the 2^32-bit rationale.
- contracts / infra: the seven blocklist guards have no tests; check_dependencies.py does and ignores dev edges.
  verdict: CONFIRMED
  evidence: `grep -rl check_ tools/tests firmware/tools/tests` → test_bench_ingest.py, test_suite_registry.py (not guard tests), test_ui_snapshot_manifest.py, test_resource_guard.py, test_dependencies.py; firmware/tools/check_dependencies.py:55-56 skips `kind == "dev"`.
- maps: rust.obc-map-scene and rust.obcm-testkit are registry suites that run zero tests.
  verdict: CONFIRMED
  evidence: grep '#[test]' firmware/obc-map-scene/src = 0 (382 LOC); host/obcm-testkit/src = 0 (1,247 LOC); both have `command = cargo test -p … --locked` entries.
- device-system: CoreHarness in the conformance test is a ~530 LOC third executor.
  verdict: CONFIRMED (approximately)
  evidence: host/obc-host-core/tests/device_core_conformance.rs: `struct CoreHarness` at 231, `impl CoreHarness` 247-627, `impl TraceHarness<Action> for CoreHarness` 628-~720, second `impl CoreHarness` at 989; file 2,104 LOC, corpus 1,486.
- infra: policy-file edits (CLAUDE.md, AGENTS.md, tools/justfile …) select every workflow-owned suite (25 jobs).
  verdict: PLAUSIBLE, not re-run
  evidence: tools/suite_registry.py:63-78 TEST_POLICY_PATTERNS lists .config/nextest.toml, .github/workflows/**, testing/**, suite_registry.py, ci_aggregate.py, docs/testing.md, CONTRIBUTING.md, AGENTS.md, CLAUDE.md, tools/justfile, tools/obc, tools/obc-dev.sh. `select` takes only --base/--head, so simulating a CLAUDE.md edit needs a commit (forbidden here).
## owner_questions_unanswered
- Which #1449 decisions are you overturning? The infra survey's headline proposal (job map instead of registry+selector, -3,300 LOC, no level taxonomy on the CLI, no coverage scaffold) directly contradicts L1-L6 and L8 in the plans survey. No survey presents this as a single yes/no; a redesign cannot start until you say whether the taxonomy, the per-suite registry, the `obc test unit|component|…` interface and the coverage ratchet survive.
- What is the numeric target? Surveys measure (obc-app edit ~72 s incl. clippy on your Mac; CI test job 233 s; ios-app 13-18 min) but none states the goal you would accept: e.g. 'an app-local edit tests in <10 s', 'CI wall <3 min', 'no PR job over 5 min'. Without a target the proposals cannot be ranked.
- Is one shared host 'Device' library (App + HostLoop + Mem stores + scripted ports, and separately the wasm/stdio virtual flat-store device for TS/Swift) allowed to exist as permanent product-adjacent code? Four surveys converge on it, but #1495 rejected 'new test-support crates or helper modules' and the scaffolding-stripped rule would otherwise apply. Where does it live (host/ or apps/), who owns it, and what is its death trigger if any?
- What does the 'glance' actually look like and where do you read it — the GitHub run page (job names + one summary table), a local `obc` command, or a committed file? No survey designs the single view of surfaces × levels × pass/duration that requirement 5 asks for; they only note that today's 69-row table and the 26 job names are not it.
- Which cross-surface system tests are the acceptance set? The nine surveys propose ~45 candidates (map PBF→card→App frame; event-pack service tree→client→device frame; TS client→Rust engine; Swift client→Rust engine; real-browser assemble; desktop launch; conformance corpus under BOARD_SUPPORT; snapshot sweep as one Rust test). You need to pick the must-haves and their cadence, because each costs build-graph weight (new crate depending on obc-pack, obcm-assemble, obc-host-core, obc-app).
- Which tests may be deleted outright rather than moved? Candidates found across surveys total ~6,000+ LOC: retired S0 tests (~900), ~60 XCUITest methods (~1,600), OBCMockTests (~600), nowcast_skill_events + nowcast_cost (1,132), feeders.rs + its gate (326), obc-app tests/dirty.rs (242), s6b soak (999), bench_ingest (534), LOC ledger scripts+tests (~1,650), ble_central_repro/display_test bins (528), legacy-library compat pins (~350). Requirement 1 says 'may be deleted'; the redesign needs your per-item yes.
- Golden artifacts: keep committed pixel/byte/counter goldens at all (ui-snapshots.sha256 churns 19×/21 days, obc-bench golden.txt, web-assemble expected/*.obcm, resource_baseline.json with 166 KB of prose)? If yes, under one regeneration convention (e.g. OBC_UPDATE_GOLDENS=1) — this is a policy choice no survey can make.
- One feature shape or two? Dropping `--all-features` so local `-p` and CI share artifacts implies deleting the six `external-fixtures` Cargo features and auto-syncing ~4 MB (grimsel/monaco) and 38 MB (weather) fixture packages on first local run. Is network-on-first-run acceptable for `obc test -p obc-route`, and must the device feature shape be the only shape firmware crates are tested in?
- What is the concrete HIL/phone slot: a registered `hardware` suite with manual cadence whose command is one small VCOM/stdio Link driver (and #1177's smoke-upload built on it), or issue-comment evidence as today? And should #91 be reopened/relabelled since none of its deliverables exist while #1177 re-specifies them?
- Do the board crate's three on-device binaries count as benches (separate never-shipped bins rule), scaffolding (strip), or CI inputs? flat_store_bench is the frame-ceiling ELF resource_guard measures in CI (ci.yml:594), so it cannot simply be deleted; ble_central_repro and display_test are past their issues.
- Coverage (TS5): keep the ratchet plan, defer it, or drop it? Your six requirements never mention coverage, but #1448 Gate 5 is blocked on TS5/TS6 and coverage-policy.toml + ~30 validator lines + 52 `coverage_component` fields exist for it.
- Does the ~2 s first-launch cost per freshly linked test binary (measured 44-46 s per obc-app edit, 0 CPU) occur in your own terminal? It is the single largest local lever after binary consolidation and only you can check/adjust it (a system privacy setting).
- Is release.yml's verification (tag↔version guard, committed-test-key refusal, mkimage inspect gate) in scope for the 'organized, standardized' system, and should the registry model a `release` cadence for it, or is it explicitly out of the test system?