diff --git a/firmware/obc-route/Cargo.toml b/firmware/obc-route/Cargo.toml
index 1269d359..10e79374 100644
--- a/firmware/obc-route/Cargo.toml
+++ b/firmware/obc-route/Cargo.toml
@@ -11,6 +11,7 @@ description = "OBCR route format reader + GPX converter (no_std) — shared by t
 # does (its scratch is a `.bss` static), so `alloc` stays off by default.
 alloc = []
 external-fixtures = []
+nav-metrics = []
 
 [dependencies]
 heapless = "0.8"
diff --git a/firmware/obc-route/src/nav.rs b/firmware/obc-route/src/nav.rs
index f49c2e94..6a82627c 100644
--- a/firmware/obc-route/src/nav.rs
+++ b/firmware/obc-route/src/nav.rs
@@ -491,6 +491,10 @@ pub struct NavScratch<const N: usize = NAV_MAX_NODES> {
     /// is always re-seeded to a real rung before any heap op runs (see [`NavEntry::f`]).
     eps_num: u16,
     eps_den: u16,
+    #[cfg(feature = "nav-metrics")]
+    probes: u64,
+    #[cfg(feature = "nav-metrics")]
+    full_misses: u64,
 }
 
 // Table budget, enforced at compile time; slot indices — heap positions and
@@ -505,7 +509,18 @@ impl<const N: usize> NavScratch<N> {
         assert!(N > 0 && N < HEAP_NONE as usize);
         // `eps_num`/`eps_den` are `0` here (the `.bss`/zeroed-init contract; `f` degrades safely on
         // `0/0`) and set to a real [`NAV_EPSILON_LADDER`] rung by the first search's re-seed.
-        NavScratch { entries: [NavEntry::EMPTY; N], heap: [0; N], used: 0, heap_len: 0, eps_num: 0, eps_den: 0 }
+        NavScratch {
+            entries: [NavEntry::EMPTY; N],
+            heap: [0; N],
+            used: 0,
+            heap_len: 0,
+            eps_num: 0,
+            eps_den: 0,
+            #[cfg(feature = "nav-metrics")]
+            probes: 0,
+            #[cfg(feature = "nav-metrics")]
+            full_misses: 0,
+        }
     }
 
     /// Allocate a zeroed `NavScratch` **directly on the heap**, never on the stack.
@@ -528,6 +543,12 @@ impl<const N: usize> NavScratch<N> {
         unsafe { alloc::boxed::Box::<Self>::new_zeroed().assume_init() }
     }
 
+    /// Table slot probes and failed lookups in a full table, cumulative across retries.
+    #[cfg(feature = "nav-metrics")]
+    pub fn probe_stats(&self) -> (u64, u64) {
+        (self.probes, self.full_misses)
+    }
+
     fn reset(&mut self) {
         for e in self.entries.iter_mut() {
             e.meta = 0;
@@ -538,9 +559,13 @@ impl<const N: usize> NavScratch<N> {
 
     /// Linear-probe lookup. Bounded at `N` probes: with no deletions ever, an occupied
     /// run can only end at a free slot — the full-table bound only guards corruption.
-    fn lookup(&self, id: u32) -> Option<usize> {
+    fn lookup(&mut self, id: u32) -> Option<usize> {
         let mut i = id as usize % N;
         for _ in 0..N {
+            #[cfg(feature = "nav-metrics")]
+            {
+                self.probes += 1;
+            }
             let e = &self.entries[i];
             if !e.occupied() {
                 return None;
@@ -550,6 +575,10 @@ impl<const N: usize> NavScratch<N> {
             }
             i = (i + 1) % N;
         }
+        #[cfg(feature = "nav-metrics")]
+        {
+            self.full_misses += 1;
+        }
         None
     }
 
@@ -561,7 +590,14 @@ impl<const N: usize> NavScratch<N> {
             return Err(NavError::Exhausted);
         }
         let mut i = id as usize % N;
-        while self.entries[i].occupied() {
+        loop {
+            #[cfg(feature = "nav-metrics")]
+            {
+                self.probes += 1;
+            }
+            if !self.entries[i].occupied() {
+                break;
+            }
             i = (i + 1) % N;
         }
         self.entries[i] =
diff --git a/host/obc-bench/Cargo.toml b/host/obc-bench/Cargo.toml
index 515440e9..7b633e9c 100644
--- a/host/obc-bench/Cargo.toml
+++ b/host/obc-bench/Cargo.toml
@@ -6,7 +6,7 @@ publish = false # host-only bench harness — never shipped or published
 description = "Host render benchmark harness + CI pixel-hash and read-counter golden gate over the real obc-reader → obc-render pipeline (issues #327, #1467)"
 
 [features]
-nav-metrics = ["obc-reader/nav-metrics"]
+nav-metrics = ["obc-reader/nav-metrics", "obc-route/nav-metrics"]
 
 [dependencies]
 obc-map-scene = { path = "../../firmware/obc-map-scene" }
diff --git a/host/obc-bench/examples/nav_cost.rs b/host/obc-bench/examples/nav_cost.rs
index c83e025e..aacb7b74 100644
--- a/host/obc-bench/examples/nav_cost.rs
+++ b/host/obc-bench/examples/nav_cost.rs
@@ -108,6 +108,11 @@ fn main() {
         assert!(index.total_distance_m > 0);
         std::fs::write(&args[7], &sink.0).unwrap();
     }
+    #[cfg(feature = "nav-metrics")]
+    {
+        let (probes, full_misses) = scratch.probe_stats();
+        println!("{{\"table_slot_probes\":{probes},\"table_full_misses\":{full_misses}}}");
+    }
     let stats = cache.stats();
     let measured_calls = src.calls.get();
     let measured_bytes = src.read_bytes.get();
