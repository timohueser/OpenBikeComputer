# Build output
/target

# Flashable image (regenerated by `cargo run` / elf2uf2-rs)
*.uf2

# Baked test tile — large + regenerable from the source .pbf (see README / plan).
# Cropped via: osmium extract --bbox=7.83,47.982,7.87,48.008 ... | obcm_pack.py
/fixtures/*.obcm
