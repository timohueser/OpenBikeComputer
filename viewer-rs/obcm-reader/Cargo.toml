[package]
name = "obcm-reader"
version = "0.1.0"
edition = "2021"
description = "OBCM map format reader (no_std + alloc) — pure-parse logic for the desktop simulator and nRF5340 firmware"

[features]
default = ["std"]
std = []

