[package]
name = "obcm"
version = "0.1.0"
edition = "2021"
description = "OBCM map format reader (no_std + alloc) — shared by the desktop simulator and the nRF5340 firmware"

[features]
default = ["std"]
std = []
