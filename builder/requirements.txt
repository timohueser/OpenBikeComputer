# Web builder (builder/server) runtime deps. The map packer itself is the
# native `obc-pack` binary (host/obc-pack) — no Python packing deps needed.
# The Geofabrik half lives in requirements-static.txt: the site deploy needs it
# without the server, and one file means the versions cannot drift apart.
-r requirements-static.txt
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
