# Local maintainer host for the schema editor. Product builds use the static
# frontend and the published cell catalog; raw OSM processing lives in obc-bake.
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
