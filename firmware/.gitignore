# Cargo build output — the workspace root and any nested crate (e.g. the MCU
# benches on the mcu-render-bench branch, which are separate workspaces).
target/

# obc-mkimage / DFU sideload output
UPDATE.BIN
obc-fw-nrf54l/app.bin
