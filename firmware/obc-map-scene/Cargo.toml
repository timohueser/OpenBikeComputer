[package]
name = "obc-map-scene"
version = "0.1.0"
edition = "2021"
publish = false
description = "Allocation-free semantic map-scene seam shared by map sources and the renderer"

[dependencies]
heapless = "0.8"
libm = { version = "0.2", default-features = false }
