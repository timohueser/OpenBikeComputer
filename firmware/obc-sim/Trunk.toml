# Trunk configuration for the web (wasm) build of the simulator.
# Paths are resolved relative to this file's directory, so `trunk` can be invoked
# from anywhere via `--config firmware/obc-sim/Trunk.toml`.

[build]
target = "index.html"
dist = "dist"

[serve]
addresses = ["127.0.0.1"]
port = 8080
