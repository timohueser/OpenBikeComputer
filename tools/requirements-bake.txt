# Official OSM replication client used by `obc bake --all` after the initial
# planet snapshot. Keep this separate from the web-builder server dependencies.
osmium>=4.3.1,<5

# The reference archive ingest tool (`host/obc-dem/reference/ingest.py`). rasterio brings its
# own GDAL wheel, so a bake box needs no system GDAL.
rasterio>=1.3,<2
pyproj>=3.6,<4
numpy>=1.26,<3
